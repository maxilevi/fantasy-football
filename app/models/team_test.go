package models

import "testing"

func TestTeamValue(t *testing.T) {
	/*players := make([]Player, 20)
	for i := 0; i < len(players); i++ {
		players[i] = RandomPlayer()
	}
	team := Team{
		Players: players,
	}
	var sum int32
	for i := range players {
		sum += players[i].MarketValue
	}
	if sum != team.MarketValue() {
		t.Error("Team value is not summed player value")
	}*/
}